struct Point {
    x: float,
    y: float
}

fn Point.new(x: float, y: float) -> Point {
    return Point { x: x, y: y }
}

fn Point.getX(self) -> float {
    return self.x
}

fn Point.getY(self) -> float {
    return self.y
}

point := Point.new(1.0, 1.0)
print(point.x)