# Test file for signature help feature

fn add(x: int, y: int) -> int {
    return x + y
}

fn multiply(a: int, b: int, c: int) -> int {
    return a * b * c
}

fn greet(name: str) -> str {
    return "Hello, " + name
}

# Test function calls - signature help should appear when typing:

result2 := multiply(2, 3, 4)
result3 := greet("World")
