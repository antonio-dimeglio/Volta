struct Point {
    x: float,
    y: float
}

# Struct instantiation
p := Point { x: 3.0, y: 4.0 }

# Method syntax
fn Point.distance(self: Point) -> float {
   return self.x * self.x + self.y * self.y
}

dist := p.distance()