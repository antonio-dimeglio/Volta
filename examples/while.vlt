fn sum_to_n(n: i32) -> i32 {
    let mut total := 0
    let mut i := 1
    while i <= n {
        total = total + i
        i = i + 1
    }
    return total
}

fn main() -> i32 {
    let mut result := sum_to_n(10)
    # print("\t")
    # print(result)
    # print("\n")
    return result
}